{% extends 'base.html.twig' %}

{% block title %}Profil{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col">
                {{ component('profileForm', {
                    user: user
                    }) }}
            </div>
        </div>
    </div>
{% endblock %}
