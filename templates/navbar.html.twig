<nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('home_index') }}">TaskSphere</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Projets
                    </a>
                    <ul class="dropdown-menu">
                        {# <li><a class="dropdown-item" href="{{ path('project_show', { 'keyCode': app.user.selectedProject.keyCode }) }}">
                                {{ app.user.selectedProject }}
                            </a>
                        </li> #}
                        <li><hr class="dropdown-divider"></li>
                        <li><a class="dropdown-item" href="{{ path('project_list') }}">Voir tous les projets</a></li>
                        <li><a class="dropdown-item" data-bs-target="#modalCreateProject" data-bs-toggle="modal" href="#">Créer un projet</a></li>
                    </ul>
                </li>
                <li class="nav-item">
                    <a class="nav-link" data-bs-target="#modalCreateIssue" data-bs-toggle="modal" href="#">Créer</a>
                </li>
            </ul>
            <div class="d-flex" role="search">
                <div class="dropdown">
                    <button class="btn btn-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                        {{ app.user }}
                    </button>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{ 'user_show' }}">Mon profil</a></li>
                        <li><a class="dropdown-item" href="{{ '/logout' }}">Se déconnecter</a></li>

                    </ul>
                </div>
            </div>
        </div>
    </div>
</nav>
